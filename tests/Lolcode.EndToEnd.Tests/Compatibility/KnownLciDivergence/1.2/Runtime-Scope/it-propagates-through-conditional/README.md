# Pinned lci divergence

Category: `pinned-lci-semantic-gap`

Pinned-lci evidence: The 1.2 control-flow clause keeps its pre-branch `IT` value rather than propagating the enclosing function/main `IT`. dotnet-lolcode intentionally follows the 1.2 scoping rule and verifies the propagated value through this fixture.
