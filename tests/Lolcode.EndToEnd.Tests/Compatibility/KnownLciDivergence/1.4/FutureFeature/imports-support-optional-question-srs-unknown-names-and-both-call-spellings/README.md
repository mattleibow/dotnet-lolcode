# Pinned lci divergence

Category: `pinned-lci-parser-gap`

Pinned-lci evidence: Pinned lci rejects the optional-question unknown import with `variable is not an array: STRING`; the ordinary STRING import and both call spellings are shared.
